#!/usr/bin/python3

def pow(a, b):
    """
    Compute the power of a number.

    Args:
        a (int): The base number.
        b (int): The exponent.

    Returns:
        int: The result of raising `a` to the power of `b`.
    """
    return a ** b
